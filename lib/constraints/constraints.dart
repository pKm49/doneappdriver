import 'package:flutter/material.dart';

//constant size used for just spacing
Widget kHeight(double height) {
  return SizedBox(
    height: height,
  );
}

Widget kWidth(double width) {
  return SizedBox(
    width: width,
  );
}
